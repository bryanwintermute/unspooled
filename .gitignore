__pycache__/
*.pyc
*.pyo

# Pytest cache (generated by pytest, not source).
.pytest_cache/

# Local virtualenvs (created during dev / tests). Not source.
venv/
.venv/

# Editor / OS detritus
.DS_Store
*.swp

# Copilot CLI session transcripts (auto-generated locally; not source).
copilot-session-*.md
*~

# Captures from local RE sessions (if you replicate the technique).
/tmp/rongta-writes/
